# Development Rules

- Default scope: `packages/coding-agent/` unless the user names another package.
- “agent” means the coding-agent product, not this assistant.
- GitHub: NEVER comment or create issues unless the user supplies exact content.
- Git: NEVER commit unless asked.
